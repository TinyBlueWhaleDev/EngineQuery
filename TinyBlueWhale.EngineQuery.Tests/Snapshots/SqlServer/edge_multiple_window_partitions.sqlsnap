SELECT [o].[order_id] AS [OrderId], [o].[user_id], [o].[total], ROW_NUMBER() OVER (PARTITION BY [o].[user_id]
ORDER BY [o].[total] DESC) AS [UserRowNumber], NTILE(@p0) OVER (ORDER BY [o].[total] DESC) AS [GlobalQuartile]
FROM [orders] AS [o]

-- Parameters
@p0 = 4